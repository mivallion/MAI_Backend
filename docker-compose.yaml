version: '3.9'

services:
  db:
    image: postgres:13-alpine
    volumes:
      - ./volumes/postgresql:/var/lib/postgresql/data/
    env_file:
      - ./.env.db
  nginx:
    build: ./nginx
    volumes:
      - ./public:/home/app/web/public
    ports:
      - "80:80"

